import React from "react";
import ReactDOM from "react-dom";
import App from "./App"; // Đảm bảo đúng đường dẫn
import "./index.css"; // Optional: file css cho giao diện của bạn

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
